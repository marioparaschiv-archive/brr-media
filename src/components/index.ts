/*
 * Export components for cleaner imports like so:
 *
 * import { ErrorBoundary } from './components';
 */

export { default as ErrorBoundary } from './ErrorBoundary';
export { default as Holidays } from './Holidays';